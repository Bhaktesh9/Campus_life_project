{
  "name": "campus-life-demo",
  "version": "0.1.0",
  "description": "Campus Life web app demo for IST4035 coursework",
  "scripts": {
    "test": "jest --coverage",
    "start": "live-server --port=8080 --open=./index.html",
    "lint": "npx eslint . --ext .js"
  },
  "devDependencies": {
    "eslint": "^9.39.1",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^30.2.0",
    "madge": "^8.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}
